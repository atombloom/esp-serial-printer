# 串口打印机对接组件
* 对接串口打印机模块

## 支持功能
1. 支持查询打印机设备温度(返回有效数值即可)
2. 支持走纸(n步)
3. 支持打印测试图片(支持流控参数，下位机缓存区有限)
4. 测试图片用c数组，写入.h文件，可简单生成一个条纹测试图
5. 设计独立任务模式，加入状态管理，避免阻塞上层应用

## 代码要求
1. 参考项目风格，模块化设计, 解耦好维护 
2. 仅保留必要注
